Functions in pixg
---------
	
Util:
	tablelength(t)
		returns table content count
	sign(v)
		returns -1, 0 or 1
	clamp(x, min, max)
		returns clamped x
	btf(b)
		bool to float (true = 1, false = 0)
	isBetween(a, min, max)
		returns bool
		
Angles:
normalizeAngle(a)
	returns an angle between 0 and 360
angleBetween(v1, v2)
	returns the angle between 
isAngleBetween(n,a,b)
	is angle n between a and b?
angleDif(targetA, targetB)
	returns difference between angles



Tables:
	addAt(t, i, d)
		returns table t with data d added at i
	addAtEnd(t, d)
		returns table t with data d added at end
	removeNil(t)
		returns t without nils in the middle
	tableContains(t, v)
		returns bool